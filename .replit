modules = ["nodejs-22"]

# Bind to 0.0.0.0 so Replit's webview can reach the dev server.
run = "npm run dev -- --hostname 0.0.0.0 --port 3000"

[[ports]]
localPort = 3000
externalPort = 80

[nix]
channel = "stable-24_05"
